// A tuple is a fixed length array where each element has a particular type
// Useful mostly for key value pairs, coz it's not ideal to have more than 2 value

// E.G #1
let person: [number, string] = [1, 'xolani'];
